# 开发日志

> 记录每日攻克的技术难点与解决方案，是最真实的学习记录。

---

## 2026.03.25 — 首次成功连接星闪开发板与手机蓝牙

**成果**：终于将星闪开发板的BLE与手机端成功连接。

**难点**：不知道与蓝牙相关的驱动文件在SDK中的具体位置。

**解决方案**：通过在SDK目录中全局搜索关键字（`grep -r "ble_" .`），逐步定位相关驱动文件。

---

## 2026.03.27 — C++在海思SDK中的编译问题

**问题**：在海思SDK编译环境中运行C++代码时，链接阶段报错。

**根本原因**：C++编译链依赖Linux平台的libc，而LiteOS是极小的轻量级平台，不提供这些依赖。

**解决思路**：禁用C++异常（`-fno-exceptions`）、RTTI（`-fno-rtti`），并提供缺失的运行时符号。

---

## 2026.03.29 — MPU6050硬件接口调通

**成果**：解决MPU6050 I2C接口配置问题，成功读取传感器数据。

**主要问题**：
1. 开发板硬件原理图标注不清晰，迟迟找不到I2C时钟线和数据线的GPIO编号。
2. 最终在SDK源码的宏定义中找到答案：**GPIO15（SCL）、GPIO16（SDA）**。
3. MPU6050 I2C地址兼容性问题（官方SDK代码与物理原理图不一致）。

**经验**：遇到硬件接口问题，优先查SDK源码中的宏定义，而非原理图。

---

## 2026.03.30 — 解决X/Y/Z数据读取 + 引入TinyML

**成果1**：修复了MPU6050三轴数据读取错误。

**坑点**：以为数据寄存器地址固定为 `0x3B`，实际上需要从 `0x3B` 开始**连续读取14字节**，而不是每次单独读一个寄存器。

**成果2**：为解决"突然躺下"和"跌倒"的误判问题，决定引入 **TinyML（端侧机器学习）**。准备数据集：`normal.csv` 和 `fall.csv`。

---

## 2026.03.31 — 电源独立供电方案

**需求**：做机器学习测试时需要将整个系统绑在身上，必须独立供电。

**方案**：锂电池 + **MH-CD42充放电模块**（小巧，有稳定5V输出）。

**新问题**：电池休眠问题（轻载时MH-CD42自动进入休眠，系统断电）。

**解决方向**：增加假负载或使用唤醒引脚保持模块工作。

---

## 2026.04.01 — C/C++混合调用

**问题**：如何在C代码开发环境中调用C++？

**关键**：使用 `extern "C"` 包裹C++接口声明，并注意CMakeLists中的路径配置。

---

## 2026.04.02 — 里程碑：C++成功移植到LiteOS！

**里程碑**：C++编译链成功在LiteOS平台上运行！

**核心解决方案**：
- 添加 `-fno-exceptions -fno-rtti -fno-use-cxa-atexit` 编译选项
- 实现 `operator new/delete` 使用LiteOS内存分配接口
- 提供 `__cxa_atexit`、`__dso_handle` 的空实现

---

## 2026.04.08 — 明确SLE与BLE的使用策略

**洞察**：
- SLE（星闪）与BLE是**完全不同**的通信协议，不能混用API。
- 当前SLE仅在部分华为手机与星闪开发板间支持，普通设备不支持。
- **策略**：用两块开发板之间的SLE通信来验证星闪协议，板对手机用BLE。

---

## 2026.04.11 — 里程碑：跌倒推理全链路跑通！

**里程碑**：经过多天调试，成功解决推理链路上的所有问题！

**调试方法**：一层一层分析代码，从应用层→推理引擎→特征提取，逐层排查。

---

## 2026.04.15 — 理解推理窗口机制

**理解**：开发板启动后**不是立即开始推理**，而是先采集满 `100×3` 帧数据（填满buffer），之后用滑动窗口方式持续推理。

**新问题思考**：如果长时间未发生跌倒，数据会一直循环覆盖buffer，这是合理的。数据保存在RAM中的循环缓冲区，不需要持久化存储。

---

## 2026.04.20 — 数据集问题 + SisFall集成

**问题1**：自采数据不完整，部分跌倒样本质量差。

**问题2**：如何将公开数据集 **SisFall Enhanced** 合理嵌入到系统中。

**关键挑战**：SisFall的采样率与开发板采样率对齐问题（重采样/插值处理）。

---

## 2026.04.21 — 系统学习SLE与BLE原理

专门花一天系统学习星闪SLE协议与BLE蓝牙的底层原理，建立协议栈的完整知识框架。

---

## 2026.04.22 — BLE连接建立失败

**问题**：服务端与客户端互相扫描发现后，无法正式建立连接。

**排查方向**：检查广播包类型是否为 `connectable undirected`（可连接广播）。

---

## 2026.04.23 — BLE连接后立即断开

**问题**：连接建立后立刻断开。

**排查方向**：连接参数配置问题，特别是监督超时（supervision timeout）设置过短。

---

## 2026.04.24 — SLE与雷达并行供电问题

**问题**：SLE协议栈与毫米波雷达同时运行时，供电不足导致底层物理崩溃（随机重启）。

**解决方案**：加入延时，错开SLE与雷达的峰值电流时间点，或升级供电方案（增大滤波电容）。

---

## 2026.04.26 — SLE 星闪通知机制调试全记录

**目标**：实现跌倒检测服务端（板A）检测到跌倒时，向星闪客户端（板B）推送通知，客户端打印 `FALL` 并点亮 LED。

### 问题1：客户端 `notification_cb` 永远不触发

**现象**：服务端 `ssaps_notify_indicate` 返回 SUCCESS，日志显示 `[SLE Server] Fall alert sent successfully!`，但客户端完全无反应，只打印心跳 `[CLIENT] 1`。

**调试过程**：
1. 首先怀疑 SLE 连接问题 → 客户端心跳正常，连接本身无误。
2. 对比官方 `sle_speed_client` 示例发现：官方在 MTU 协商完成后立即调用 `ssapc_find_structure` 进行服务发现，我们的代码跳过了这一步。

**根本原因**：WS63 SLE SSAP 协议栈要求客户端完成服务发现（`ssapc_find_structure`），才会将服务端的 Notify 包路由到应用层的 `notification_cb`。缺少这一步，通知在协议栈内部被静默丢弃。

**修复**：在 MTU 协商回调 `my_sle_exchange_info_cbk` 中补充调用服务发现：

```c
ssapc_find_structure_param_t find_param = {0};
find_param.type     = SSAP_FIND_TYPE_PRIMARY_SERVICE;
find_param.start_hdl = 1;
find_param.end_hdl  = 0xFFFF;
ssapc_find_structure(client_id, conn_id, &find_param);
```

**结果**：服务发现成功（`start_hdl=0x10 end_hdl=0x11`），但通知仍未收到，问题未彻底解决。

---

### 问题2：服务发现成功后仍收不到通知

**现象**：客户端日志打印了 `Service discovery complete`，但跌倒后依然没有 `FALL` 输出。

**根本原因**：SLE SSAP 与 BLE 一样存在 **CCCD（Client Configuration Descriptor，客户端配置描述符）** 机制。服务端属性必须注册 `SSAP_DESCRIPTOR_CLIENT_CONFIGURATION` 描述符，协议栈才会为每条连接维护"通知订阅状态"。我们的服务端缺少这一步，导致 `ssaps_notify_indicate` 虽然返回成功，但数据在协议栈层面被丢弃。

**对比官方示例**（`sle_speed_server.c`）发现：官方在 `ssaps_add_property_sync` 之后还额外调用了 `ssaps_add_descriptor_sync`，我们的代码完全缺少这一步。

**双重修复方案**：

**① 服务端**（`sle_server_task.c`）：在 property 注册后、启动服务前添加 CCCD 描述符，初始值 `0x01` 表示默认开启通知：

```c
ssaps_desc_info_t descriptor = {0};
static uint8_t cccd_val[2] = {0x01, 0x00};
descriptor.permissions        = SSAP_PERMISSION_READ | SSAP_PERMISSION_WRITE;
descriptor.operate_indication = SSAP_OPERATE_INDICATION_BIT_READ | SSAP_OPERATE_INDICATION_BIT_WRITE;
descriptor.type               = SSAP_DESCRIPTOR_CLIENT_CONFIGURATION;  // 0x02
descriptor.value              = cccd_val;
descriptor.value_len          = 2;
ssaps_add_descriptor_sync(server_id, service_handle, property_handle, &descriptor);
```

**② 客户端**（`sle_client_task.c`）：服务发现完成后向服务端写一次激活字节，激活通知路由通道（与官方 `sle_speed_client` 示例行为一致）：

```c
// find_structure_cmp_cbk 中
static uint8_t s_activate = 0x01;
ssapc_write_param_t wr = {0};
wr.handle   = g_found_service_start_hdl;
wr.type     = SSAP_PROPERTY_TYPE_VALUE;
wr.data_len = 1;
wr.data     = &s_activate;
ssapc_write_req(client_id, conn_id, &wr);
```

---

### 问题3：客户端收到通知有明显延迟

**现象**：跌倒后客户端不会立即打印 `FALL`，存在数秒延迟。

**根本原因**：延迟不在 SLE 通信侧（<100ms），而完全来自 AI 推理管道：

| 阶段 | 耗时 |
|------|------|
| DSP 特征提取（Edge Impulse） | ~3045 ms |
| TFLite 神经网络推理 | ~2798 ms |
| **单次推理合计** | **~5.8 秒** |

推理在主线程同步执行，期间传感器完全停止采样。最坏情况下（跌倒发生在上一轮推理刚开始时），延迟可达 **~12 秒**。

**决策**：维持现有 AI 方案，TinyML 端侧推理是本项目核心亮点。当前延迟属于 RISC-V 无硬件 DSP 加速的性能特性，后续可通过将推理移至独立线程改善采样连续性，或在 Edge Impulse 平台重训更小窗口的模型降低推理时间。

---

### 最终通信链路（修复后）

```
[板A - Server]                              [板B - Client]
  MPU6050 100Hz 采样
      ↓ 每 30 个样本触发
  Edge Impulse TFLite 推理 (~5.8s)
      ↓ 跌倒概率 > 80%
  ssaps_notify_indicate(data=0x05)
      ─────── SLE SSAP Notify ──────────→   notification_cb 触发
                                               GPIO_09 LED 点亮
                                               串口打印 "FALL"
                                               ssapc_write_req(0x06) ACK 回传
      ←───── ssaps_write_request_cbk ──────  
  打印 "ACK received from client!"
```

---

### 本次涉及技术栈

| 层次 | 技术 / API |
|------|-----------|
| 芯片 | HiSilicon WS63（RISC-V 32 位） |
| 操作系统 | LiteOS v208.5.0 |
| 通信协议 | SLE SSAP（星闪服务访问协议） |
| 服务端关键 API | `ssaps_add_property_sync`、`ssaps_add_descriptor_sync`、`ssaps_notify_indicate` |
| 客户端关键 API | `ssapc_find_structure`、`ssapc_write_req`、`ssapc_register_callbacks` |
| AI 推理框架 | Edge Impulse SDK + TensorFlow Lite Micro |
| 传感器 | MPU6050（I²C，GPIO15=SDA，GPIO16=SCL） |
| 告警输出 | GPIO_09 LED + UART 串口打印 |

---

## 2026.04.30 — 中文注释乱码修复 + 编码规范落地

**问题现象**：`main_task.c`、`sle_client_task.c`、`ws2812b.c/.h` 中中文注释显示为 `鏄...`、`涓...`、`�?` 等乱码，阅读和维护成本很高。

**根本原因**：
1. 历史文件发生过“UTF-8 按 GBK 解码后再次保存”的链式编码错误（典型 Mojibake）。
2. 部分注释尾部包含不可逆损坏字符（`�`），说明原始字节信息已经丢失，无法 100% 自动还原。

**本次修复策略**：
- 先按 GBK→UTF-8 方向做批量回转，恢复大部分中文注释可读性；
- 对不可逆字符统一替换为 `?`，避免继续传播乱码；
- 将相关文件统一保存为 **UTF-8（无BOM）**，并将该规则纳入项目工程规范。

**涉及代码文件**（设计与实现相关）：
- `application/samples/my_demo/fall_detect/src/main_task.c`
- `application/samples/my_demo/fall_detect/src/sle_client_task.c`
- `application/samples/my_demo/fall_detect/src/ws2812b.c`
- `application/samples/my_demo/fall_detect/inc/ws2812b.h`

**修复脚本示例**（PowerShell）：

```powershell
$files = @(
  'application\samples\my_demo\fall_detect\inc\ws2812b.h',
  'application\samples\my_demo\fall_detect\src\main_task.c',
  'application\samples\my_demo\fall_detect\src\ws2812b.c',
  'application\samples\my_demo\fall_detect\src\sle_client_task.c'
)
$gbk = [System.Text.Encoding]::GetEncoding(936)
$utf8NoBom = New-Object System.Text.UTF8Encoding($false)
foreach ($p in $files) {
  $text  = Get-Content $p -Raw -Encoding UTF8
  $fixed = [System.Text.Encoding]::UTF8.GetString($gbk.GetBytes($text))
  $fixed = $fixed -replace '�?','?' -replace '�','?'
  [System.IO.File]::WriteAllText((Resolve-Path $p), $fixed, $utf8NoBom)
}
```

**验证方式**：
- 使用 `rg` 检索常见乱码特征串（如 `鏄`、`涓`、`锟`、`�`）；
- 逐文件抽查关键注释段，确认主要逻辑注释恢复可读；
- 对残留 `?` 位置人工复核并按语义补全（后续持续清理）。

**本次新增技术栈/工程能力**：

| 层次 | 技术 / 工具 |
|------|------------|
| 文本编码治理 | UTF-8（无BOM）统一保存策略 |
| 编码修复工具 | PowerShell + .NET Encoding（GBK:936 / UTF-8） |
| 质量检查 | `rg` 乱码特征检索 + 人工语义复核 |

---

## 2026.05.01 — WS2812B 灯带告警功能集成与调试

**目标**：在跌倒检测触发后，灯带进入红色闪烁告警；平时保持熄灭，不影响主业务线程。

**遇到的问题**：
1. **颜色异常/全白/不亮**：WS2812B 对高低电平脉宽非常敏感，初始循环计数与板子主频不匹配。  
2. **告警不稳定**：在主线程里直接控制灯带，容易和采样/推理节奏互相干扰。  
3. **复位时序不充分**：发送帧前后低电平保持时间不足，导致偶发花屏或首帧丢失。  

**解决办法**：
- 在 `ws2812b.c` 中按 WS63 实测主频重新调参 `T1H_CNT/T0H_CNT`，并采用 `O0 + volatile` 循环确保延时不被优化。  
- 增加独立 `ws2812b_flash_task` 任务：  
  - `g_ws2812b_alert_active = false` 时空转；  
  - `true` 时执行“红灯 400ms / 熄灭 400ms”循环。  
- 在发送跌倒告警成功后，设置 `g_ws2812b_alert_active = true`，并使用 `WS2812B_ALERT_HOLD_MS` 自动超时关闭。  
- 在初始化阶段增加上电稳定与复位低电平保持，避免上电初期随机色。  

**对应设计代码**：
- `application/samples/my_demo/fall_detect/src/ws2812b.c`（位级时序驱动、闪烁任务）
- `application/samples/my_demo/fall_detect/inc/ws2812b.h`（接口与时序调参说明）
- `application/samples/my_demo/fall_detect/src/main_task.c`（告警状态联动与自动关闭）
- `application/samples/my_demo/fall_detect/src/sle_client_task.c`（客户端告警联动）

**结果**：
- 跌倒触发后可稳定看到红色闪烁；  
- 空闲时灯带不误亮，主循环采样与通信链路保持稳定；  
- 告警可自动结束，避免长期占用视觉提示。

**本次新增技术栈**：

| 层次 | 技术 / API |
|------|-----------|
| 灯带驱动 | WS2812B 单总线位时序（bit-banging） |
| 引脚控制 | `uapi_gpio_set_dir`、`uapi_gpio_set_val` |
| 任务调度 | LiteOS `osThreadNew` + `osDelay` 周期闪烁 |
| 告警联动 | `g_ws2812b_alert_active` + `WS2812B_ALERT_HOLD_MS` |
| 系统协同 | AI推理结果与SLE/BLE告警同步触发灯效 |

---

*持续更新中...*

---

## 2026.05.13 - V100C / Air780EHV 4G DTU 电话报警链路打通

**目标**：在 Board B 收到 SLE 跌倒报警 `0x05` 后，通过 UART1 向银尔达 V100C / Air780EHV 发送 JSON，由 V100C 自动拨打家属电话。

**关键结果**：极简拨号脚本已验证 V100C、SIM 与 `cc.dial()` 可用；正式 UART 任务脚本接收 WS63 JSON 后，报警时电话已打通。

**主要问题与解决办法**：

1. **V100C 后台入口不明确**：确认入口为 `https://www.yinerda.com/dtupt`，路径为 `参数设置 -> 分组管理 -> fall -> 参数配置 -> 任务`。
2. **任务脚本格式错误**：银尔达任务必须第一行是 `function`、最后一行是 `end`；不能直接粘贴带注释头或 `sys.taskInit(function() ... end)` 的普通 LuatOS 脚本。
3. **先分层验证电话能力**：新增 `tools/v100c_min_call_test.lua`，确认极简拨号能响铃，从而排除 SIM、电话功能和参数同步问题。
4. **正式脚本无固定手机号**：`tools/V100C_PASTE_THIS_TO_TASK1.lua` 只解析 WS63 JSON 中的 `phone` 字段；真实号码在本地 WS63 工程 `fall_alert_4g_dtu_config.h` 中配置，不提交到 GitHub。
5. **旧测试号码残留导致多拨号**：清空任务框、删除多余任务，只保留正式 UART 任务即可。

**新增文件**：

- `tools/v100c_min_call_test.lua`：V100C 电话能力最小测试脚本。
- `tools/v100c_uart_call_task.lua`：正式 UART JSON 触发拨号脚本。
- `tools/V100C_PASTE_THIS_TO_TASK1.lua`：后台任务1可直接粘贴版本。
- `docs/06-系统集成/V100C_Air780EHV_UART_call_debug_2026-05-13.md`：完整流程、问题与解决办法记录。

---

## 2026.05.07 - Edge Impulse + SisFall 模型部署到 WS63 的完整调试

**目标**：将 SisFall 二分类模型部署到 WS63 + LiteOS 开发板，并在串口实时输出 `normal` / `FALL_RISK` 状态，通过 SLE 发送 SOS 告警。

**关键模型参数**：`256 samples`、`6 axes`、`200 Hz`、`5 ms interval`，输入轴为 `accX + accY + accZ + gyrX + gyrY + gyrZ`。

**主要问题与解决方案**：

1. **AI 没有运行**：固件处于 Client 模式，MPU6050 与 Edge Impulse 推理只在 Server 模式执行；已强制传感器板运行 Server 模式。
2. **`run_classifier()` 返回 `-22`**：Edge Impulse 生成的全局 C++ impulse handle 在 LiteOS 启动时没有可靠构造；改为运行时 placement-new 构造 `ei_impulse_handle_t`。
3. **推理时间字段异常**：EI SDK 计时函数未适配 WS63 平台，去掉 `dsp/infer` 耗时打印。
4. **模型输出极端化**：SisFall 数据与 MPU6050 实测数据存在尺度差异，特别是陀螺仪单位；对 gyro 使用 `dps / 250.0f` 归一化。
5. **静止时 `ai_fall` 仍可能很高**：确认是模型 domain mismatch，因此最终状态不直接采用 AI 分数，而加入物理门控。
6. **静止或轻微动作误触发 gate**：显式配置 MPU6050 的 DLPF、采样率、加速度量程和陀螺仪量程，并增加陀螺仪零偏校准。
7. **单个异常点影响滑动窗口**：加入 `hits` 机制，要求多个采样点满足冲击/失重/旋转条件；再加入连续窗口 `confirm=2/2` 后才报警。

**最终串口判断格式**：

```text
[AI] status=normal ai_fall=99% normal=1% gate=NO confirm=0/2 peak_acc=1.10G min_acc=1.09G peak_gyr=0.00 hits=0/0/0
[AI] status=FALL_RISK ai_fall=100% normal=0% gate=YES confirm=2/2 peak_acc=3.20G min_acc=0.24G peak_gyr=3.12 hits=7/2/28
```

**结论**：工程上已达到比赛演示可用状态。静止状态不报警，模拟跌倒/撞击/翻转可触发 `FALL_RISK`。但模型本身仍存在板端数据分布不匹配，后续产品级方案建议使用 WS63 + MPU6050 自采数据重新训练。

详细记录：[`docs/04-TinyML模型/EdgeImpulse_WS63_部署调试记录_2026-05-07.md`](../04-TinyML模型/EdgeImpulse_WS63_部署调试记录_2026-05-07.md)

**补充训练侧记录**：已整理 Edge Impulse 平台训练全过程，包括 SisFall 二分类归并、CSV 格式、Label/Category 区分、上传数量差异原因、`1280 ms / 256 samples / 200 Hz / 6 axes` Impulse 配置依据、Spectral Analysis 选择理由、Classifier 训练评估原则、C++ Library/EON/INT8 导出和板端参数一致性检查。

训练过程文档：[`docs/04-TinyML模型/EdgeImpulse_训练数据全过程_2026-05-07.md`](../04-TinyML模型/EdgeImpulse_训练数据全过程_2026-05-07.md)

---

## 2026.05.15 — 修复WS2812B灯带数据时序失真，灯光报警恢复正常

**目标**：让跌倒报警时 8 颗 WS2812B 灯珠稳定显示纯红闪烁（待机绿色），解决报警时颜色错乱、后段灯珠不亮的问题。本次修复同时取代了 `2026.05.01` 条目里"`O0 + volatile` 循环 + 实测主频调参 `T1H_CNT/T0H_CNT`"的旧时序方案。

**故障现象**：报警时颜色不是纯红、像任意颜色乱跳；只有前 2 颗灯珠在闪，第 3、4 颗亮但不闪，后 4 颗完全不亮。

**根本原因**：旧实现用 `nop` 计数循环模拟位时序延时，经验值是按 `-O0` 调出的；但发送函数 `ws2812b_send_byte()` 实际标了 `__attribute__((optimize("O2")))`，O2 下延时循环被大幅收紧，实际脉宽只有预期的 1/2 ~ 1/3，远低于 WS2812B 规格。首颗灯尚能勉强解码，误差沿灯带逐颗累积，后段数据彻底失真 —— 表现为颜色乱、灯珠不亮。

**解决方案**：

1. 弃用 `nop` 计数延时，改用 RISC-V `cycle` 周期计数器（`csrr cycle`）忙等计时，时长与编译优化档位完全无关。
2. CPU 核心频率在本工程不是编译期固定常量，故不硬编主频；改为上电时用 TCXO 微秒计数器自动标定 `cycles/µs`，再把纳秒级时序换算成周期数。
3. 位时序配置由"魔法 nop 圈数"（`T1H_CNT` 等）改为纳秒宏（`WS2812B_T0H_NS` 等），可读、可移植。
4. 顺带把报警闪烁从 5 Hz 调整为 2.5 Hz（200 ms 红 / 200 ms 灭），更符合"警报"节奏、更醒目。

**对应设计代码**：

- `application/samples/my_demo/fall_detect/src/ws2812b.c`（周期计数器延时、主频标定、位发送、闪烁任务）
- `application/samples/my_demo/fall_detect/inc/ws2812b.h`（ns 级位时序、闪烁周期配置）

**关键结果**：复位后 8 颗灯珠全部绿色；触发跌倒报警后 8 颗灯珠同步转为红色闪烁，颜色纯正、无错乱、无掉灯。开机串口新增标定日志 `[WS2812B] calibrate: ... cyc/us ...`，可核对 CPU 主频。

**经验**：位带（bit-bang）这类时序敏感代码不要依赖 `nop` 圈数，应优先用硬件计数器计时；主频非编译期常量时，用独立可信时基在运行时自标定。

详细记录：[`docs/02-硬件驱动/WS2812B灯光报警驱动.md`](../02-硬件驱动/WS2812B灯光报警驱动.md)

---

## 2026.05.18 — 集成自采腰部数据模型，构建 B+NN 混合判定

**目标**：把用 WS63+MPU6050 腰部佩戴自采数据训练的 Edge Impulse 卷积网络模型（工程 `fall_ws63_waist`，二分类 `{fall, normal}`）真正集成进固件，并与已有的确定性序列检测器（路径 B）组成「**路径 B 触发 + 神经网络复核否决**」的混合判定。本次取代 `2026.05.07` 基于 SisFall 公开数据集、存在板端数据分布不匹配的旧部署。

**为什么是「B 触发、NN 否决」**：新模型测试准确率 91.67%、误报率 0%、但漏检率 14.3%（召回 85.7%）。模型「不误报、却会漏检」——这个错误特性决定了 NN 只能用来**否决**路径 B 的误报，不能用来**单独拍板**（否则漏检的 14.3% 直接丢失）。最终策略：路径 B 确认跌倒后调用 NN 复核，仅当 NN 以 ≥80% 高置信度判 `normal` 才否决报警；NN 同意/不可用/置信度不足一律照常报警。阈值设得高是刻意保守——跌倒检测里漏报代价远大于误报。

**集成过程遇到并解决的 6 个问题**：

1. **备份目录污染编译**：固件 `CMakeLists.txt` 用 `GLOB_RECURSE` 递归收集 `*.cpp`，旧模型备份目录恰好在 `src/` 下被一并编译。解决：所有备份移出 `src/` 目录树。
2. **移植层缺口**：EI SDK 要求用户实现 `ei_printf`/`ei_malloc`/计时等函数，其自带各平台实现全被 `#if EI_PORTING_<平台>` 包起来，WS63 不匹配任何已知平台。解决：新写 `ei_porting_ws63.cpp` 集中显式提供全部移植函数，计时用 WS63 原生 `uapi_tcxo_get_us()`。
3. **posix 链接符号冲突**：链接报 `ei_read_timer_ms/us` 重复定义。根因——WS63 RISC-V 工具链预定义了 `__unix__`，导致 `EI_PORTING_POSIX` 被自动启用、`posix/ei_classifier_porting.cpp` 实际参与了编译（这也解释了旧工程 `ei_printf` 的来源——一直由 posix 弱符号提供）。解决：`CMakeLists.txt` 的 `UNWANTED_PORTS` 追加排除 `posix/*.cpp`。
4. **C++ 全局构造函数不可靠**：WS63 启动不保证执行 C++ 全局构造，不能直接用生成的 `ei_default_impulse`。解决：用 placement new 显式构造 `ei_impulse_handle_t`，调用带 handle 版本的 `run_classifier`。
5. **量纲一致性**：模型按训练 CSV 的量纲（加速度 milli-g ×1000、角速度 centi-dps ×100）训练，`EI_Fall_Push()` 推理时严格做同样换算，否则模型输出全错。
6. **系统黑盒、无可观测性**：路径 B 停在 `IDLE` 时一行日志不打，晃动传感器毫无反应、无从判断板子状态。解决：`fall_algo.c` 新增 `Fall_Algo_State_Name()`，`main_task.c` 加每秒一次 `[Monitor]` 状态日志（B 状态机状态 + 本秒 \|acc\| 范围 + \|gyro\| 峰值 + 队列水位）。

**对应设计代码**：

- 新增 `inc/ei_fall.h`、`src/ei_fall.cpp`（600 样本环形缓冲 + `run_classifier` 封装）
- 新增 `src/ei_porting_ws63.cpp`（WS63 移植层 + 64KB 静态内存池）
- 修改 `src/main_task.c`（接入 B+NN 混合判定、状态监控、关闭 `FALL_LOG_MODE`）
- 修改 `src/fall_algo.c` / `inc/fall_algo.h`（新增状态查询）
- 修改 `src/CMakeLists.txt`（排除 posix 移植）
- 替换 `edge-impulse-sdk/`、`model-parameters/`、`tflite-model/` 为新模型导出库

**关键结果**：固件编译、链接通过并正常启动；`[EI] NN ready` 表示推理器就绪；200Hz 采样零丢样；系统堆实测 `free` 约 130KB，新增约 113KB 静态内存（64KB 内存池 + 28.8KB 缓冲 + 20KB tensor arena）放得下。`[Monitor]` 日志使板子状态实时可见。澄清了一个非缺陷现象：**手晃传感器不报警是正确行为**——路径 B 要求「失重≥120ms → 冲击 → 冲击后静止 1.2s」三段严格连续，手晃既凑不够持续失重、冲击后也不静止；真实触发需让模块自由下落并落地静置。

**经验**：① 集成第三方 SDK 先吃透其移植层/弱符号机制，自己显式接管而非赌平台自动探测；② 工具链预定义宏（`__unix__`）会暗中改变库行为，需逐一核对；③ 嵌入式 C++ 别依赖全局构造函数；④ 训练与部署的数据量纲必须一致；⑤ 可观测性要在设计时留出，确定性算法在「无事发生」时也要有心跳日志，否则「没反应」与「坏了」分不清。

详细记录：[`docs/04-TinyML模型/EdgeImpulse_模型集成与B+NN混合判定_2026-05-18.md`](../04-TinyML模型/EdgeImpulse_模型集成与B+NN混合判定_2026-05-18.md)

**补充：采样时序原理**。整理了「为什么采样必须精准 200Hz、最初用 `osDelay` 为何做不到、如何用硬件定时器解决」的前因后果：`osDelay` 参数单位是 tick 而非毫秒，WS63 配置 `LOSCFG_BASE_CORE_TICK_PER_SECOND=100` 即 1 tick=10ms，故 `osDelay(10)` 实为 100ms；tick 时基分辨率不足、且相对延时会累积漂移，根本无法表达 5ms 周期。模型按固定 200Hz 训练，采样时间轴失真等同于喂错数据。解决方案是硬件定时器（µs 级、独立于 OS tick）+ 中断只发信号量 + 采样/推理双任务 + 队列缓冲，并给出端到端时延预算。

采样时序文档：[`docs/06-系统集成/精准采样时序_Tick原理与硬件定时器采样管线.md`](../06-系统集成/精准采样时序_Tick原理与硬件定时器采样管线.md)
