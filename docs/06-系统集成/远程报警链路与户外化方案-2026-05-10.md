# 远程报警链路、户外化方案与问题记录（2026-05-10）

> 本文记录跌倒检测系统从“本地声光报警”扩展到“手机端远程报警”的完整过程，包括 Wi-Fi 网关、后端服务、PushPlus 微信推送、腾讯云短信受阻原因，以及户外佩戴产品化时的 4G Cat.1/DTU 方案选型。

## 1. 背景与目标

原始跌倒检测系统已经可以在 WS63 开发板上完成：

```text
MPU6050 六轴数据采集
    -> Edge Impulse TinyML 跌倒判断
    -> SLE 星闪发送跌倒信号
    -> Board B 本地蜂鸣器 / LED / WS2812B 报警
```

新的目标是：

```text
只要检测到跌倒，手机端就能收到报警通知。
```

进一步产品化目标是：

```text
佩戴者在户外运动时，不依赖旁边电脑、不依赖局域网、不依赖旁边手机，也能自动通知家属。
```

因此系统被拆成两个阶段：

| 阶段 | 目标 | 通信路径 |
| --- | --- | --- |
| 比赛演示版 | 快速稳定展示手机收到报警 | Board A -> SLE -> Board B -> Wi-Fi -> 后端 -> PushPlus 微信 |
| 户外产品版 | 人在户外也能独立报警 | 可穿戴设备 -> 4G Cat.1/DTU -> 云端 -> 手机通知 |

## 2. 当前工程中的跌倒信号位置

结合工程代码，跌倒信号来自以下链路：

| 功能 | 文件/位置 | 说明 |
| --- | --- | --- |
| AI 跌倒判断 | `application/samples/my_demo/fall_detect/src/ai_model.cpp` | `fall_risk` 概率、运动门限、连续窗口确认 |
| 主循环读取 IMU | `application/samples/my_demo/fall_detect/src/main_task.c` | 调用 `AI_Feed_And_Predict_6Axis()` |
| 跌倒报警字节 | `main_task.c` 中 `FALL_ALERT_PAYLOAD` | 当前为 `0x05` |
| SLE Server 发送 | `sle_server_task.c` | `sle_send_fall_alert()` -> `ssaps_notify_indicate()` |
| SLE Client 接收 | `sle_client_task.c` | 收到 `data[0] == 0x05` 后触发本地报警 |
| SLE ACK | `sle_client_task.c` | Client 回写 `0x06` 给 Server |

关键结论：**最合适的远程报警接入点是 Board B 的 SLE Client 收到 `0x05` 后。**

原因：

- Board A 已承担传感器采样、AI 推理和 SLE Server，不宜继续叠加复杂联网逻辑。
- Board B 是接收网关，适合扩展 Wi-Fi/4G 上报能力。
- SLE 回调中不能做阻塞网络请求，因此只投递事件，由独立任务上报。

## 3. Wi-Fi 网关实现方案

### 3.1 设计结构

比赛演示版采用：

```text
Board A：跌倒检测节点
    MPU6050 + AI + SLE Server
        ↓ SLE 0x05
Board B：报警网关
    SLE Client + 蜂鸣器/灯带 + Wi-Fi HTTP
        ↓ POST /api/fall-alert
电脑/服务器后端
        ↓ PushPlus / 后续短信语音
手机端
```

### 3.2 新增/修改文件

在 WS63 工程中新增：

```text
application/samples/my_demo/fall_detect/inc/fall_alert_gateway.h
application/samples/my_demo/fall_detect/inc/fall_alert_gateway_config.h
application/samples/my_demo/fall_detect/src/fall_alert_gateway.c
```

并在：

```text
application/samples/my_demo/fall_detect/src/sle_client_task.c
```

接入：

```c
fall_alert_gateway_init();
fall_alert_gateway_post_fall(data->data[0]);
```

设计原则：

```text
SLE notification 回调只投递事件，不直接联网；
Wi-Fi 连接、HTTP 发送、失败重试都放到 fall_alert_gateway_task 后台任务中。
```

### 3.3 板端配置

配置文件：

```text
application/samples/my_demo/fall_detect/inc/fall_alert_gateway_config.h
```

示例：

```c
#define FALL_GATEWAY_WIFI_ENABLED       1
#define FALL_GATEWAY_WIFI_SSID          "YOUR_2G_WIFI"
#define FALL_GATEWAY_WIFI_PASSWORD      "YOUR_WIFI_PASSWORD"

#define FALL_GATEWAY_SERVER_HOST        "192.168.1.100"
#define FALL_GATEWAY_SERVER_PORT        8080
#define FALL_GATEWAY_SERVER_PATH        "/api/fall-alert"
#define FALL_GATEWAY_DEVICE_ID          "ws63-fall-client-001"
#define FALL_GATEWAY_AUTH_TOKEN         "change-me-demo-token"
#define FALL_GATEWAY_CLOUD_COOLDOWN_MS  60000U
```

注意：

- `FALL_GATEWAY_SERVER_HOST` 必须填电脑/服务器在同一网络下的 IPv4 地址。
- 不能填 `127.0.0.1`，因为那是板子自身。
- 如果使用手机热点，电脑和 Board B 必须连接同一个热点。
- WS63 更适合使用 2.4 GHz Wi-Fi；校园网/5 GHz/隔离网络容易失败。

### 3.4 实测日志

Board B 成功连接 Wi-Fi：

```text
[FALL_GW] gateway task started.
[FALL_GW] Wi-Fi scan start: OPPO Find X9s Pro.
[FALL_GW] Wi-Fi scan done.
[FALL_GW] Wi-Fi connect start.
[FALL_GW] Wi-Fi connected.
[FALL_GW] DHCP success, ip=10.109.0.122.
```

触发跌倒后：

```text
[CLIENT] Notification: data[0]=0x05
[FALL_GW] fall event queued, payload=0x05, count=1.
[CLIENT ALERT] SOS RECEIVED!
[CLIENT] ACK write confirmed by server, status: 0x0
[FALL_GW] HTTP response: HTTP/1.0 200 OK
[FALL_GW] fall alert uploaded.
```

## 4. 后端服务实现

### 4.1 后端文件

本仓库保留可运行后端脚本：

```text
tools/fall_alert_backend_demo.py
tools/fall_alert_backend.env.example
```

后端能力：

- 接收 `POST /api/fall-alert`
- 校验 `Authorization: Bearer <token>`
- 打印报警 JSON
- 支持后端限流，默认 60 秒内重复报警不重复通知
- 支持 dry-run
- 支持 PushPlus 微信推送
- 预留腾讯云短信/语音通知接口

### 4.2 dry-run 验证

PowerShell：

```powershell
cd D:\fbb_ws63\fbb_ws63-master\src

$env:FALL_BACKEND_TOKEN="change-me-demo-token"
$env:FALL_NOTIFY_PROVIDER="dryrun"
$env:FALL_NOTIFY_DRY_RUN="1"
$env:FALL_CONTACTS="+8613800138000,+8613900139000"

python tools\fall_alert_backend_demo.py --host 0.0.0.0 --port 8080
```

后端输出：

```text
Notify provider: dryrun, dry_run=True
Rate limit: 60s
Contacts: ['+8613800138000', '+8613900139000']
Listening on http://0.0.0.0:8080/api/fall-alert
```

收到报警：

```text
FALL ALERT
{
  "device_id": "ws63-fall-client-001",
  "event": "fall",
  "payload": 5,
  "event_count": 1
}
[NOTIFY] dry-run enabled; no SMS/voice request sent.
[NOTIFY] message=检测到跌倒报警，设备 ws63-fall-client-001，时间 ...
```

## 5. 手机端通知：PushPlus 方案

### 5.1 为什么使用 PushPlus

原计划使用腾讯云短信/语音电话，但实际遇到资质限制：个人账号无法快速申请国内短信签名。

因此比赛演示版改用：

```text
后端 -> PushPlus -> 微信通知
```

优点：

- 个人可用。
- 开通快。
- 不需要短信签名和单位资质。
- 手机端可以实时收到通知。
- 不改变板端代码。

### 5.2 PushPlus 运行命令

登录 PushPlus 后获取 token：

```text
https://www.pushplus.plus/
```

PowerShell：

```powershell
cd D:\fbb_ws63\fbb_ws63-master\src

$env:FALL_BACKEND_TOKEN="change-me-demo-token"
$env:FALL_NOTIFY_PROVIDER="pushplus"
$env:FALL_NOTIFY_DRY_RUN="0"
$env:PUSHPLUS_TOKEN="YOUR_PUSHPLUS_TOKEN"
$env:PUSHPLUS_CHANNEL="wechat"
$env:FALL_RATE_LIMIT_SECONDS="10"

python tools\fall_alert_backend_demo.py --host 0.0.0.0 --port 8080
```

实测手机端收到：

```text
工单完成通知
工单名称：跌倒报警
设备名称：跌倒报警设备：ws63-fall-client-001
发起人：pushplus
发起时间：2026-05-09 17:03:13
```

这证明以下链路完整打通：

```text
Board A 跌倒检测
    -> SLE 发送 0x05
Board B 收到 0x05
    -> 本地声光报警
    -> Wi-Fi HTTP 上报
Python 后端
    -> PushPlus
手机微信
    -> 收到跌倒报警
```

## 6. 腾讯云短信/电话尝试与问题

### 6.1 原计划

原计划后端接入腾讯云：

```text
Tencent SendSms      -> 短信报警
Tencent SendTtsVoice -> 电话语音报警
```

后端已预留：

```powershell
$env:FALL_NOTIFY_PROVIDER="tencent"
$env:FALL_NOTIFY_DRY_RUN="0"
$env:FALL_NOTIFY_CHANNELS="sms"
```

以及：

```text
TENCENT_SECRET_ID
TENCENT_SECRET_KEY
TENCENT_SMS_SDK_APP_ID
TENCENT_SMS_SIGN_NAME
TENCENT_SMS_TEMPLATE_ID
TENCENT_VMS_SDK_APP_ID
TENCENT_VMS_TEMPLATE_ID
```

### 6.2 实际遇到的问题

腾讯云国内短信签名需要资质。

控制台提示：

```text
由于当前运营商不支持对个人用户进行签名报备，
自 2025年9月18日起，控制台将不再支持创建和修改个人自用资质。
个人认证用户如需申请签名，可创建他用资质。
```

当前没有学校/企业/单位资质，因此无法快速创建国内短信签名。

### 6.3 解决方案

短期比赛方案：

```text
使用 PushPlus 微信推送作为手机端报警通道。
```

长期产品方案：

```text
使用学校/企业/机构资质申请短信签名，或使用运营商 SIM 卡直接短信/电话，或接入 4G 模块独立上云。
```

答辩解释：

> 由于国内短信签名需要单位资质，比赛演示阶段采用 PushPlus 微信推送完成手机端实时报警。后端采用插件化通知架构，已预留腾讯云短信和语音接口；后续具备学校或企业资质后可无缝切换为短信和电话报警。

## 7. 遇到的问题与解决方案汇总

| 问题 | 现象 | 原因 | 解决方案 |
| --- | --- | --- | --- |
| Wi-Fi 网关没有工作 | 日志提示 `Wi-Fi gateway disabled` | `FALL_GATEWAY_WIFI_ENABLED=0` | 改为 `1` 后重新编译烧录 |
| disabled 日志刷屏 | 每隔几秒重复打印 | 后台任务循环重试 | 改为只打印一次 |
| Board B 连不上 Wi-Fi | assoc fail / DHCP fail | 热点不稳定、密码错误、5 GHz、校园网隔离 | 使用手机 2.4 GHz 热点或小路由器 |
| 后端收不到报警 | Board B 上传失败 | `SERVER_HOST` 填了旧网段 IP 或防火墙拦截 | 确认电脑与 Board B 同一热点，填电脑热点 IPv4，允许 8080 端口 |
| 不能填 `127.0.0.1` | Board B 访问失败 | `127.0.0.1` 是板子自己 | 填电脑局域网 IP |
| 腾讯云短信开通受阻 | 签名无法申请 | 个人账号无单位资质 | 改用 PushPlus；保留腾讯云接口 |
| 真实户外不可靠 | 电脑/手机热点不能常伴随 | Wi-Fi 方案只是演示原型 | 产品化改为 4G Cat.1/DTU + GNSS |
| SLE 回调不宜联网 | 可能阻塞协议栈 | 网络请求耗时不可控 | 回调只投递事件，后台任务负责上报 |
| 重复报警 | 一次跌倒可能多次触发 | AI 窗口和通信重试 | Board A 冷却 + 后端限流 |

## 8. 户外佩戴产品化方案

### 8.1 为什么当前 Wi-Fi 方案不够

当前比赛演示版依赖：

```text
Board B 附近有 Wi-Fi/手机热点
电脑或服务器正在运行后端
```

但真实产品中，佩戴者可能在户外运动。因此设备必须具备：

```text
广域联网能力
独立供电能力
定位能力
断网重试能力
误报撤销能力
```

### 8.2 推荐产品架构

```text
可穿戴跌倒检测设备
    ├── WS63 / 主控
    ├── MPU6050 / 六轴 IMU
    ├── TinyML 跌倒判断
    ├── 4G Cat.1 / DTU 模块
    ├── GPS + 北斗定位
    ├── 蜂鸣器 / 震动马达 / LED
    ├── SOS 按键 / 误报取消按键
    └── 锂电池 + 充电管理 + 大电流电源
```

通信链路：

```text
检测到跌倒
    -> 本地蜂鸣器倒计时
    -> 用户未取消
    -> 4G Cat.1 上报云端
    -> 后端 PushPlus / APP / 短信 / 电话
    -> 家属手机收到通知和位置
```

### 8.3 4G 模块选型：DTU 固件 vs AT 固件

| 对比项 | DTU 固件 | AT 固件 |
| --- | --- | --- |
| 联网参数 | 工具/网页配置 | MCU 逐条 AT 指令配置 |
| 联网维护 | 模块自动维护 | MCU 手动维护 |
| TCP/MQTT/HTTP | 多数支持透传 | 需要按协议手动 AT 配置 |
| 开发难度 | 低 | 高 |
| 适合场景 | 比赛、快速原型、稳定透传 | 深度定制、完全控制模块 |

结论：

```text
比赛/快速户外验证：优先 DTU 固件。
后续深度开发：再考虑 AT 固件。
```

### 8.4 推荐购买

推荐购买：

```text
M100MG(DTU固件) 贴片卡-带GPS+北斗 测试套装
供电电压：5-12V
```

理由：

- DTU 固件，适合串口透传快速上云。
- 带 GPS + 北斗，适合户外跌倒定位。
- 测试套装更适合调试，通常包含底板/天线/线材。
- 5-12V 供电更容易调试，4G 模块瞬时电流更稳定。

不推荐：

- 纯 AT 固件版本：开发量大。
- 不带 GPS 版本：户外报警缺少位置信息。
- 裸模块：电源、SIM、天线、串口调试麻烦。

下单前建议询问卖家：

```text
这个“M100MG(DTU固件)贴片卡-带GPS+北斗测试套装，5-12V供电”是否支持串口透传到 HTTP/MQTT/TCP 服务器？是否带 4G 天线、GPS/北斗天线和测试底板？
```

### 8.5 与当前工程如何衔接

当前上层接口保持不变：

```c
void fall_alert_gateway_post_fall(uint8_t source_payload);
```

后续可以扩展多个底层实现：

```text
fall_alert_wifi.c      // 当前 Wi-Fi HTTP 上报
fall_alert_4g_dtu.c    // 未来 4G DTU 串口透传
fall_alert_4g_at.c     // 未来 AT 指令控制
fall_alert_local.c     // 本地蜂鸣器/灯带/震动
```

4G DTU 版本的逻辑：

```text
Board / MCU 收到跌倒事件
    -> UART 输出 JSON
    -> DTU 模块自动通过 4G 发送到服务器
```

示例串口数据：

```json
{"device_id":"ws63-wearable-001","event":"fall","payload":5,"lat":30.000000,"lng":120.000000}
```

## 9. 比赛展示建议

建议现场演示三层反馈：

```text
1. Board B 本地蜂鸣器 + WS2812B 红灯
2. 电脑后端显示 FALL ALERT 日志
3. 手机微信收到 PushPlus 跌倒报警
```

答辩时强调：

- 本系统采用端侧 AI，不依赖云端识别。
- SLE 用于低功耗近距离可靠传输。
- 后端采用插件化通知架构，可切换 PushPlus、短信、语音电话、APP 推送。
- 个人短信资质受限，因此比赛阶段用微信推送；产品化阶段接入单位短信资质或 4G Cat.1 独立上云。
- 户外版本将加入 4G DTU、GPS/北斗定位、电池管理、误报撤销和 SOS 按键。

## 10. 当前成果状态

已完成：

- [x] Board A 跌倒检测
- [x] SLE 发送 `0x05`
- [x] Board B SLE 接收和 ACK
- [x] Board B 本地声光报警
- [x] Board B Wi-Fi 连接和 DHCP
- [x] HTTP POST 上报后端
- [x] 后端 token 校验和限流
- [x] PushPlus 微信推送到手机
- [x] 腾讯云短信/语音接口预留
- [x] 户外 4G DTU 产品化方案确定

待完成：

- [ ] 购买并验证 M100MG DTU + GPS/北斗测试套装
- [ ] 增加 `fall_alert_4g_dtu.c`
- [ ] 将定位信息加入报警 JSON
- [ ] 增加 SOS 按键和误报取消按键
- [ ] 设计电池供电与低电量报警
- [ ] 若获得单位资质，启用腾讯云短信/语音通道

## 11. V100C / Air780EHV DTU 电话短信方案（2026-05-10 补充）

### 11.1 新购买模块与结论

本次购买/咨询的模块信息重点如下：

```text
V100C / Air780EHV DTU 固件
支持任务和数据模板代写
示例 1：服务器下发 JSON 后主动呼出电话
示例 2：DTU 固件发送短信测试
重要限制：默认配套物联网卡通常只支持流量，不支持打电话和发短信；如需电话/短信，必须插入自己的普通手机 SIM 卡。
```

结合当前工程，结论是：

```text
DTU 可以作为 Board B 后面的 4G 电话/短信执行器；
WS63 不负责蜂窝协议栈，只在跌倒发生时通过 UART 给 DTU 发一行 JSON；
DTU 的 Lua 任务解析 JSON 后调用 cc.dial() 或短信 API。
```

也就是说，户外化后的报警链路可以变成：

```text
Board A：MPU6050 + TinyML 跌倒检测
    -> SLE 发送 0x05
Board B：SLE Client 收到 0x05
    -> 本地蜂鸣器 / WS2812B 报警
    -> UART 发送 JSON 到 V100C/Air780EHV DTU
DTU：解析 JSON
    -> 4G 普通手机卡呼叫家属 / 发送短信 / 后续也可上云
家属手机
    -> 接到电话或短信
```

这个方案解决了 Wi-Fi 演示版的核心不足：**佩戴者在户外时不再依赖电脑、局域网或旁边手机热点，只依赖蜂窝网络覆盖和 SIM 卡能力。**

### 11.2 与当前 WS63 工程的接入点

仍然沿用当前已经验证的跌倒信号入口：

```text
application/samples/my_demo/fall_detect/src/sle_client_task.c
```

当 SLE Client 收到：

```c
data->data[0] == 0x05
```

当前已经做了：

```text
1. 本地声光报警
2. ACK 回写 0x06
3. Wi-Fi 网关 fall_alert_gateway_post_fall(data->data[0])
```

下一步增加 4G DTU 时，不要推翻现有架构，只增加一个并行出口：

```c
fall_alert_4g_dtu_post_fall(data->data[0]);
```

建议新增文件：

```text
application/samples/my_demo/fall_detect/inc/fall_alert_4g_dtu.h
application/samples/my_demo/fall_detect/inc/fall_alert_4g_dtu_config.h
application/samples/my_demo/fall_detect/src/fall_alert_4g_dtu.c
```

并保持接口简单：

```c
void fall_alert_4g_dtu_init(void);
void fall_alert_4g_dtu_post_fall(uint8_t source_payload);
```

这样比赛现场仍可使用 Wi-Fi + PushPlus，后续户外版再打开 DTU UART 出口，两个通道互不冲突。

### 11.3 WS63 到 DTU 的串口协议

第一版协议建议使用换行结束的 JSON，原因是调试简单，PC 串口助手也能直接模拟。

最小呼叫命令：

```json
{"cmd":"call","phone":"13800138000"}
```

串口实际发送时必须带换行：

```text
{"cmd":"call","phone":"13800138000"}\r\n
```

后续可扩展为跌倒报警事件：

```json
{"cmd":"fall_alert","phone":"13800138000","device":"ws63-fall-client-001","event":"fall","payload":5}
```

DTU 侧可以兼容两种命令：

```text
cmd=call       -> 直接拨号
cmd=fall_alert -> 记录为跌倒报警，然后拨号/短信/上云
```

### 11.4 DTU 侧 Lua 任务如何从 UART 获取 WS63 JSON

卖家给的电话 demo 是从服务器下行通道取数据：

```lua
local ndata = PronetGetRecChAndDel(1)
local j = json.decode(ndata)
if j and type(j) == "table" and j.cmd and j.phone then
    if j.cmd == "call" then
        outgoing_number = j.phone
        makeCall()
    end
end
```

要接 WS63，需要把数据源从服务器下行改成 UART 接收：

```lua
uart.setup(UART_ID, 115200, 8, 1, uart.NONE)
uart.on(UART_ID, "receive", function(id, len)
    local data = uart.read(id, len)
    -- 缓存到 rxbuf，按 \n 切行，json.decode(line)
    -- 若 cmd == "call" 或 "fall_alert"，调用 cc.dial(0, phone)
end)
```

OpenLuat 官方 API 依据：

- `cc` 通话控制模块提供 `cc.init()`、`cc.dial()`、`cc.hangUp()`、`cc.accept()`、`cc.lastNum()` 等接口。
- `uart` 串口模块提供 `uart.setup()`、`uart.on()`、`uart.read()` 等接口。

因此从能力上判断：**只要购买的 DTU 固件允许运行自定义 Lua 任务，并且外部 UART 可用，DTU 就可以通过串口获取 WS63 发来的 JSON，再触发电话/短信。**

需要到货后实测确认的点：

| 待确认项 | 为什么重要 | 建议验证方法 |
| --- | --- | --- |
| DTU 是否允许刷写/运行自定义 Lua 任务 | 不允许则不能直接写 UART 解析任务 | 问卖家或用 Luatools/DTU 工具查看任务功能 |
| 外部 UART 是 UART1 还是其他 ID | Lua 代码里的 `UART_ID` 必须对应真实引脚 | 先用 PC 串口助手发 JSON 测试 |
| UART 电平是 3.3V TTL 还是 5V TTL | WS63 GPIO 通常不能直接承受 5V | 看模块手册；若 5V 使用电平转换 |
| UART0 是否被日志/下载占用 | 避免和固件下载口冲突 | 优先使用卖家标注的用户串口 |
| SIM 卡是否支持电话/短信 | 物联网卡常常不能打电话 | 插自己的普通手机卡测试 |
| 4G 信号与供电是否稳定 | 通话瞬时电流大，供电不足会重启 | 独立 5V/12V 电源，接 4G 天线 |

### 11.5 DTU 侧参考任务文件

仓库中新增参考文件：

```text
tools/v100c_uart_call_task.lua
```

用途：

```text
PC 串口助手 / WS63 通过 UART 发送一行 JSON；
DTU Lua 任务按行解析 JSON；
收到 {"cmd":"call","phone":"手机号"} 或 {"cmd":"fall_alert","phone":"手机号"} 后拨打电话。
```

注意：该文件是面向 V100C/Air780EHV DTU 的参考任务，真实运行前需要根据卖家手册修改：

```text
UART_ID
波特率
是否需要 PronetStopProRecCh(1)
音频通道和音量
短信 API 名称
```

### 11.6 接线与供电

推荐接线：

```text
WS63 TX  -> DTU RX
WS63 RX  <- DTU TX
WS63 GND -> DTU GND
```

供电注意：

```text
不要从 WS63 的 3.3V 给 DTU 供电。
DTU 使用独立稳定电源，按模块要求选择 5V/12V。
4G 模块通话和入网瞬间电流较大，供电不足会表现为重启、掉线、无响应。
```

如果 DTU 串口是 5V TTL，必须增加电平转换；如果是 3.3V TTL，通常可以与 WS63 直接串口连接，但仍要共地。

### 11.7 到货后的严谨测试顺序

不要一上来就接 WS63，建议按下面顺序逐步验证：

| 步骤 | 测试对象 | 目标 |
| --- | --- | --- |
| 1 | DTU 单独测试 | 插普通手机 SIM、接天线、确认能注册 4G 网络 |
| 2 | 卖家电话 demo | 用服务器下发 `{"cmd":"call","phone":"手机号"}`，确认能拨号 |
| 3 | DTU UART 测试 | 用 PC 串口助手发送 `{"cmd":"call","phone":"手机号"}\r\n`，确认能拨号 |
| 4 | WS63 UART 输出测试 | Board B 串口发送固定 JSON，DTU 能收到 |
| 5 | 跌倒联调 | Board A 触发跌倒，Board B 收到 `0x05` 后驱动 DTU 拨号 |
| 6 | 户外测试 | 不开电脑、不连 Wi-Fi，只靠 SIM 卡完成报警 |

### 11.8 当前风险与比赛答辩说法

风险：

- DTU 到货前，UART ID、固件权限、任务机制不能 100% 写死。
- 默认物联网卡不能打电话/短信，必须准备普通手机 SIM 卡。
- 电话/短信比微信推送更受运营商和资费限制。
- 户外产品还需要电池、电源保护、防水外壳、误报取消和定位。

答辩建议说法：

> 当前系统已经完成端侧 TinyML 跌倒识别、SLE 传输、Board B 本地声光报警、Wi-Fi 上云和手机微信通知闭环。针对户外佩戴场景，我们设计了 4G DTU 扩展链路：Board B 在收到 SLE 跌倒字节 `0x05` 后，通过 UART 向 V100C/Air780EHV DTU 发送 JSON 指令，DTU 使用普通手机 SIM 卡触发电话或短信，从而摆脱电脑、局域网和手机热点依赖。该方案与现有 Wi-Fi 网关并行，比赛现场可稳定演示 Wi-Fi + PushPlus，产品化阶段切换或叠加 4G DTU。

### 11.9 下一步代码落地清单

WS63 工程待新增：

```text
fall_alert_4g_dtu_config.h：配置 UART、手机号、是否启用 DTU
fall_alert_4g_dtu.c：初始化 UART，跌倒时输出 JSON
sle_client_task.c：收到 0x05 后调用 fall_alert_4g_dtu_post_fall()
```

DTU 侧待验证：

```text
修改 tools/v100c_uart_call_task.lua 中 UART_ID
烧录/下发到 V100C/Air780EHV
PC 串口助手发送 JSON 验证拨号
再接 WS63 联调
```
